<h1>All Books</h1>

<% @books.each do |book| %>
  <section id="book-<%= book.id %>"
    <h2><%= link_to book.title, "/books/#{book.id}" %></h2>
    <p>Pages: <%= book.pages %></p>
    <p>Published: <%= book.published %></p>
    <p>Image: <%= book.image %></p>
  </section>
<% end %>
